# This file is part of the testthat package
# Loading testthat and running tests when devtools::test() or testthat::test_local()
# is used.

library(testthat)
library(scSampleSim)

test_check("scSampleSim")
