% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sc_simulator_fns.R
\name{create_full_augmented_data_w_num_sim_genes}
\alias{create_full_augmented_data_w_num_sim_genes}
\title{Create augmented single-cell data with a fixed number of simulated DE genes}
\usage{
create_full_augmented_data_w_num_sim_genes(
  sim_prefix,
  new_id_check,
  num_sim_genes,
  file_naming_utils,
  verbose = TRUE,
  save_down = TRUE
)
}
\arguments{
\item{sim_prefix}{Character. Simulation prefix (e.g. \code{augSimLeiden3}).}

\item{new_id_check}{Character. Run ID (e.g. \code{sim_prefix_id_nsim_500}).}

\item{num_sim_genes}{Integer. Number of simulated DE genes.}

\item{file_naming_utils}{List. Must contain \code{base_seurat_fn},
\code{sims_info_dir}, \code{param_file_prefix}, and save prefixes:
\code{de_args_save_prfx}, \code{condition_save_prfx},
\code{sample_ids_save_prfx}, \code{gt_clusterings_save_prfx}.}

\item{verbose}{Logical. If \code{TRUE}, print progress (default \code{TRUE}).}

\item{save_down}{Logical. If \code{TRUE}, save intermediate RDS files (default \code{TRUE}).}
}
\value{
A list with:
\itemize{
\item \code{used_sce}: \code{SingleCellExperiment} with real + simulated counts and \code{fake_condition} in \code{colData}.
\item \code{pa_de}: list with \code{new_id_check}, \code{set_de_genes}, \code{leiden_clusters}, file paths, etc.
}
}
\description{
Loads the base Seurat object and parameter list, runs \code{\link{prep_aug_sim}}
and \code{\link{create_augmented_data}} (from this package), then builds a
\code{SingleCellExperiment} combining real and simulated counts. Optionally
saves \code{de_args}, \code{fake_condition}, sample IDs, and ground-truth
clusterings under \code{file_naming_utils$sims_info_dir}.
}
