% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_analysis_fns.R
\name{get_mv_PVE_embeddings}
\alias{get_mv_PVE_embeddings}
\title{Multivariate embedding variance explained (cluster vs condition)}
\usage{
get_mv_PVE_embeddings(
  embeddings,
  sample_id,
  fake_condition,
  cluster,
  pc_weights = NULL,
  max_cells = Inf,
  max_pcs = Inf,
  seed = NULL
)
}
\arguments{
\item{embeddings}{Numeric matrix, cells \eqn{\times} PCs (e.g. PCA or Harmony).}

\item{sample_id, fake_condition, cluster}{Vectors of length \code{nrow(embeddings)}.
\code{sample_id} must be non-missing when \code{max_cells} is finite (stratified subsample).}

\item{pc_weights}{Optional nonnegative weights (e.g. squared PCA standard deviations).
If \code{NULL}, uses column variances of \code{embeddings}.}

\item{max_cells}{If finite, stratified subsample by \code{sample_id} to at most this size.}

\item{max_pcs}{Use only the first \code{max_pcs} columns of \code{embeddings}.}

\item{seed}{Optional RNG seed when subsampling.}
}
\value{
A list with \code{mv_tPVE} and \code{mv_sPVE} (scalars), \code{per_pc} (matrix of
incremental R^2 and TSS per dimension), \code{weights_used} (normalized), and
\code{meta} (\code{n_cells}, \code{n_cells_used}, \code{n_pcs_used}, etc.).
}
\description{
Uses multivariate \code{lm} (one QR per model) on \code{cells \times PCs} embeddings.
\strong{Sample is not a regressor:} incremental R^2 for cluster uses \code{~ condition}
vs \code{~ condition + cluster}; for condition, \code{~ cluster} vs
\code{~ cluster + condition}. Sample-driven variation therefore remains in the
embedding variance (appropriate when comparing uncorrected PCA to batch-corrected
embeddings). \code{sample_id} is still used for optional stratified subsampling.
Per-PC increments are combined with normalized \code{pc_weights} (default: column variances).
}
