% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_analysis_fns.R
\name{run_sequential_simulation_pipeline}
\alias{run_sequential_simulation_pipeline}
\title{Sequential augmented simulation through prep, data creation, and clustering}
\usage{
run_sequential_simulation_pipeline(
  base_seurat_fn,
  id_check,
  sim_prefix,
  raw_data_path,
  processed_null_data_path,
  sims_info_dir,
  param_file_prefix,
  num_de_genes,
  sim_type,
  de_args_save_prfx,
  condition_save_prfx,
  sample_ids_save_prfx,
  gt_clusterings_save_prfx,
  randomization,
  clustering,
  cut_at,
  lfc_mean,
  n_hvgs,
  use_harmony,
  leiden_resolution,
  main_covariate,
  sample_covariate,
  assay_continuous,
  cell_type_column,
  batch_covariate,
  num_pcs,
  cluster_type,
  leiden_res,
  kmeans_k,
  num_pcs_clustering,
  verbose,
  save_down
)
}
\arguments{
\item{base_seurat_fn}{Path to the base Seurat RDS used by \code{create_simulated_data}.}

\item{id_check}{Run id (integer or string); combined into \code{new_id_check} as
\code{paste0(sim_prefix, "_", id_check, "_nsim_", num_de_genes)}.}

\item{sim_prefix}{Simulation key; must match the stem of the saved pa RDS
(\code{param_file_prefix} + \code{sim_prefix} + \code{.Rds}).}

\item{raw_data_path}{Path to raw \code{SingleCellExperiment} \code{.Rda} / \code{.rds} for \code{prep_aug_sim}.}

\item{processed_null_data_path}{Path where \code{prep_aug_sim} writes the processed Seurat (via \code{dest_file}).}

\item{sims_info_dir}{Directory for simulation parameter and auxiliary RDS files.}

\item{param_file_prefix}{Prefix for the saved prep list (e.g. \code{"pa_preseurat_"}).}

\item{num_de_genes}{Number of DE genes to simulate.}

\item{sim_type}{Passed to \code{create_simulated_data}: \code{"augData"} or \code{"mSim"}.}

\item{de_args_save_prfx, condition_save_prfx, sample_ids_save_prfx, gt_clusterings_save_prfx}{Filename prefixes for intermediate saves (see \code{\link{create_simulated_data}}).}

\item{randomization}{\code{"samples"} or \code{"cells"} for \code{prep_aug_sim}.}

\item{clustering}{Clustering method for \code{prep_aug_sim} (e.g. \code{"kmeans"}, \code{"leiden"}).}

\item{cut_at}{Numeric vector of cluster-size settings for the augmentation hierarchy.}

\item{lfc_mean}{Numeric vector of mean log-fold-changes for simulated DE genes.}

\item{n_hvgs}{Number of highly variable genes in \code{prep_aug_sim}.}

\item{use_harmony}{Logical; run Harmony in \code{prep_aug_sim}.}

\item{leiden_resolution}{Leiden resolution when relevant for \code{prep_aug_sim}.}

\item{main_covariate, sample_covariate, assay_continuous, cell_type_column, batch_covariate}{\code{colData} / assay names for \code{prep_aug_sim}.}

\item{num_pcs}{Number of PCs for \code{prep_aug_sim}; also the default for clustering when
\code{num_pcs_clustering} is \code{NULL}.}

\item{cluster_type}{\code{"leiden"} or \code{"kmeans"} for \code{run_pca_harmony_leiden}.}

\item{leiden_res}{Leiden resolution for \code{run_pca_harmony_leiden}.}

\item{kmeans_k}{Number of k-means clusters for \code{run_pca_harmony_leiden}.}

\item{num_pcs_clustering}{PCs for \code{run_pca_harmony_leiden}; if \code{NULL}, \code{num_pcs} is used.}

\item{verbose}{Logical; messages from pipeline steps.}

\item{save_down}{Logical; passed to \code{create_simulated_data} (write intermediate RDS).}
}
\value{
A list with elements \code{pa} (from \code{prep_aug_sim}), \code{sim_data}
(from \code{create_simulated_data}), and \code{clust_results} (from \code{run_pca_harmony_leiden}).
}
\description{
Runs \code{\link{prep_aug_sim}} from raw \code{SingleCellExperiment} paths,
saves the parameter list to \code{sims_info_dir} as
\code{<param_file_prefix><sim_prefix>.Rds} (so \code{\link{create_simulated_data}}
can load it), calls \code{\link{create_simulated_data}}, annotates \code{pa_de}
with clustering options, and runs \code{\link{run_pca_harmony_leiden}} on
\code{used_sce}. For pseudobulk DE, call \code{\link{run_mSim_type_DE_analysis}}
on \code{result$sim_data$used_sce} separately.
}
\seealso{
\code{\link{prep_aug_sim}}, \code{\link{create_simulated_data}},
\code{\link{run_pca_harmony_leiden}}, \code{\link{run_mSim_type_DE_analysis}}
}
