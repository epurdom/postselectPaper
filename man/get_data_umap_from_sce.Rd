% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_utils.R
\name{get_data_umap_from_sce}
\alias{get_data_umap_from_sce}
\title{Compute and save UMAP coordinates from SCE (PCA or Harmony)}
\usage{
get_data_umap_from_sce(
  sce,
  dir_analysis_dump,
  reduction_type = "pca",
  num_pcs = 50,
  file_prefixes = ""
)
}
\arguments{
\item{sce}{\code{SingleCellExperiment} with \code{rowData(sce)$is_simulated}.}

\item{dir_analysis_dump}{Character. Directory to save UMAP RDS.}

\item{reduction_type}{Character. \code{"pca"} or \code{"harmony"} (default \code{"pca"}).}

\item{num_pcs}{Integer. Number of PCs / UMAP dimensions (default \code{50}).}

\item{file_prefixes}{Character. Prefix for saved filename (default \code{""}). If \code{NULL}, uses \code{file_prefixes} from the calling environment.}
}
\value{
Invisible; saves \code{<file_prefixes>umap_coords_data_<reduction_type>.rds} in \code{dir_analysis_dump}.
}
\description{
Subsets to non-simulated genes, builds a Seurat object, runs PCA or Harmony
(and PCA before Harmony if needed), then UMAP. Saves UMAP coordinates to
\code{dir_analysis_dump}.
}
