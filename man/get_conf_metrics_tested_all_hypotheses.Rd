% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_extraction_fns.R
\name{get_conf_metrics_tested_all_hypotheses}
\alias{get_conf_metrics_tested_all_hypotheses}
\title{Compute all-hypothesis (cluster-level) confusion metrics and FDP / POWER}
\usage{
get_conf_metrics_tested_all_hypotheses(
  master_pwr_table_used,
  sig_threshold_fixed,
  p_val_col_name = "adj_p_val",
  overlap_def = "union",
  cut_off_true = 0,
  cut_off_false = 0
)
}
\arguments{
\item{master_pwr_table_used}{Data frame with at least \code{id_check}, \code{overlap_count},
\code{gt_nbhd_count}, \code{found_cluster_count}, and a p-value column (see \code{p_val_col_name}).
Optionally \code{sPVE} and \code{tPVE} for average PVE of found DEs.}

\item{sig_threshold_fixed}{Numeric. Significance threshold (alpha); findings with p-value below this are "found DE".}

\item{p_val_col_name}{Character. Name of the column containing the DE-test p-value (or adjusted p-value). Default \code{"adj_p_val"}.}

\item{overlap_def}{Character. Overlap denominator: \code{"union"} (default), \code{"clust"}, or \code{"gt"}.}

\item{cut_off_true}{Numeric. Gene-cluster pairs with \code{perc_overlap > cut_off_true} are considered truly DE. Default 0.}

\item{cut_off_false}{Numeric. Gene-cluster pairs with \code{perc_overlap <= cut_off_false} are considered not DE. Default 0.}
}
\value{
A data frame with one row per \code{id_check} and columns: \code{TP}, \code{FN}, \code{FP},
\code{FN_all}, \code{POWER}, \code{POWER_all}, \code{FDP}, and optionally \code{sPVE_avg_found_DE},
\code{tPVE_avg_found_DE}. \code{FN} excludes not-tested; \code{FN_all} includes them. \code{POWER}
and \code{FDP} are set to \code{NA} or \code{0} when denominators are zero.
}
\description{
Computes true/false positives and negatives from a master power table by
defining true DE via overlap percentage and significance via a p-value column.
Aggregates TP, FN, FP, POWER, FDP (and optional PVE averages) per \code{id_check}.
}
\seealso{
\code{\link{confusion_from_overlap_matrix}}
}
