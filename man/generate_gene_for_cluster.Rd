% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lemur_framework.R
\name{generate_gene_for_cluster}
\alias{generate_gene_for_cluster}
\title{Generate one simulated DE gene for a given cluster configuration}
\usage{
generate_gene_for_cluster(
  n_clusters,
  base_expr,
  lfc_mean,
  lfc_sd = 0.5,
  sample_sd = 0.1,
  overdispersion = 0.1,
  clustering_bank,
  clust_to_sample_from,
  randomization_by_cell,
  sample,
  sf,
  nc,
  verbose = TRUE
)
}
\arguments{
\item{n_clusters}{Integer. Number of clusters (or cluster index) for this gene.}

\item{base_expr}{Numeric. Base log2 expression level.}

\item{lfc_mean}{Numeric. Mean log fold change (treatment vs control) for this gene.}

\item{lfc_sd}{Numeric. SD of log fold change (default \code{0.5}).}

\item{sample_sd}{Numeric. SD of per-sample random effect (default \code{0.1}).}

\item{overdispersion}{Numeric. Negative binomial size parameter (default \code{0.1}).}

\item{clustering_bank}{Named list of cluster assignment vectors for kmeans/leiden.}

\item{clust_to_sample_from}{Integer vector or \code{NULL}. Which cluster IDs to sample as DE cluster; \code{NULL} = all.}

\item{randomization_by_cell}{Character vector. \code{"fake_ctrl"} or \code{"fake_trt"} per cell.}

\item{sample}{Character or factor. Sample ID per cell.}

\item{sf}{Numeric vector. Cell size factors.}

\item{nc}{Integer. Number of cells.}

\item{verbose}{Logical. If \code{TRUE}, print debug messages (default \code{TRUE}).}
}
\value{
A list with \code{n_clusters}, \code{sel_cluster}, \code{is_de_cell}, \code{lfc},
\code{base_expr}, \code{log_expression_level}, \code{sample_mean}, \code{counts}.
}
\description{
Internal helper used by \code{\link{create_augmented_data}}. Generates counts
for a single gene that is differentially expressed between fake treatment and
control only within one selected cell cluster. This is a convenience wrapper
that calls \code{\link{select_de_neighborhood}} then \code{\link{generate_de_counts}}.
}
\keyword{internal}
